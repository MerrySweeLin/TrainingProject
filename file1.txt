pipeline {
    agent any

    stages {
        stage('Committed changes, trigger automated build') {
            steps {
                echo 'Committed changes, building...'
            }
        }
        stage('Trigger selected functional test scripts auto-run') {
            steps {
                echo 'Functional test scripts auto-running'
            }
        }
        stage('Scripts all pass, trigger deploying to testing env') {
            steps {
                echo 'Deploying'
            }
        }
        stage('functional test on enhancements') {
            steps {
                echo 'Functional Testing'
            }
        }
        stage('Deploy to pre-prod env, trigger regression test scripts auto-run') {
            steps {
                echo 'Regression Testing'
            }
        }
        stage('Release') {
            steps {
                echo 'Releasing'
            }
        }
    }
}
         
